{
  "name": "openopensource.github.io",
  "version": "0.0.0",
  "description": "http://openopensource.org",
  "scripts": {
    "prepublish": "make prepublish",
    "start": "make start",
    "clean": "make clean"
  },
  "devDependencies": {
    "http-server": "^0.8.0",
    "marked": "^0.3.3"
  }
}
