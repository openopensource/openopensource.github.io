{
  "name": "openopensource.github.io",
  "version": "0.0.0",
  "description": "https://openopensource.github.io/",
  "scripts": {
    "prepublish": "make prepublish",
    "start": "make start",
    "clean": "make clean"
  },
  "devDependencies": {
    "http-server": "^0.8.0",
    "marked": "^0.3.3"
  }
}
